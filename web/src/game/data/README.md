# Data Notes

This folder contains the active web game's gameplay data plus two JSON reference files copied forward from the Python prototype:

- `modules.json`
- `ships.json`

They are preserved here so the Phaser implementation can reuse the fitting/loadout definitions without depending on the archived Python codebase.
