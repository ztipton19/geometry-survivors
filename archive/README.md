# Archive

Legacy material that no longer defines the active game lives here so the repo can stay focused on the Phaser build in `web/`.

- `legacy-python/`: the previous Python prototype, related assets, and tooling
- `legacy-docs/`: older concept docs, migration notes, and superseded planning material
