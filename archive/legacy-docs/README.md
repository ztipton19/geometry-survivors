# Legacy Docs

Superseded design and migration material lives here so the root of the repo stays focused on the active Phaser build.

These files may still be useful for reference, but they no longer define the current game plan unless they are explicitly pulled forward into the web docs or build plan.
